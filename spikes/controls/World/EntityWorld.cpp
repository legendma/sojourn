#include "pch.h"
#include "EntityWorld.h"
#include "TypeSequence.h"

//template<typename Component>
//ComponentPool<Component>& EntityWorld::GetComponentPool()
//{
//    const auto index = TypeSequence<Component>::GetTypeId();
//    if( pools.size() < index + 1 )
//    {
//        pools.resize( size_t( index + 1 ) );
//    }
//
//    auto &record = pools[ index ];
//    if( record. )
//}
